const users = require('./users');
const tags = require('./tags');
const posts = require('./posts');
const postAppends = require('./post_appends');
const replies = require('./replies');

module.exports = {
  users, tags, posts, postAppends, replies,
};
